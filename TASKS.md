# Задачи проекта Baur-U-Merch

## Компонент Filteracia (Этапы фильтрации брака)

### ✅ Выполненные задачи

#### 1. Структура данных

- [x] Создан массив `data` с 5 этапами фильтрации (включая "Ваш заказ")
- [x] Добавлены поля для каждого этапа: `id`, `title`, `img`, `mobImg`
- [x] Подключены изображения для десктопной и мобильной версий

#### 2. Стилизация блоков

- [x] Конвертация CSS стилей в Tailwind CSS
- [x] Решение проблемы с `border-radius` и `border-image` (использование псевдоэлементов)
- [x] Настройка высоты блоков: динамическая высота (400px, 560px, 720px, 880px)
- [x] Настройка ширины: 4-й блок на 1.5 раза шире остальных (w-[30%] vs w-1/5)
- [x] Добавлены классы для разных размеров экрана (2xl, xl, lg, md, sm)

#### 3. Параболические линии (SVG Path)

- [x] Реализован динамический расчет координат кружков через `useRef` и `getBoundingClientRect()`
- [x] Создан SVG с параболическими путями между блоками
- [x] Использованы квадратичные кривые Безье (Q команда) для создания парабол
- [x] Настроены все параболы с изгибом вверх (amplitude: -280)
- [x] Добавлена адаптивность: автоматический перерасчет при изменении размера окна
- [x] Пунктирная линия: `stroke-dasharray: 10, 8`, `stroke-width: 4`

#### 4. Адаптивность

- [x] Десктопная версия: горизонтальное расположение блоков с параболическими линиями
- [x] Мобильная версия: сетка 2x2 с закругленными углами
- [x] Скрытие/показ элементов в зависимости от размера экрана (lg:flex hidden)

#### 5. Специальные элементы

- [x] Блок "Ваш заказ" (id: 0) с особым стилем (белый фон, без border, скрыто изображение)
- [x] Кружки на верхней части блоков (абсолютное позиционирование)
- [x] Изображения в правой части блоков с overflow-hidden

### 🔧 Технические детали

#### Используемые технологии:

- React Hooks: `useRef`, `useEffect`, `useState`, `useCallback`
- SVG Path для параболических кривых
- Tailwind CSS для стилизации
- Динамический расчет координат через `getBoundingClientRect()`

#### Ключевые функции:

- `calculatePaths()` - расчет координат и построение SVG путей
- Обработчик `resize` для перерасчета при изменении размера окна
- `requestAnimationFrame` для плавного перерасчета

### 📝 Примечания

- Параболические линии используют квадратичные кривые Безье (Q команда)
- Контрольная точка рассчитывается как середина между кружками с изгибом вверх
- Все пути динамически пересчитываются при изменении размера экрана
- SVG абсолютно позиционирован поверх блоков с `pointer-events-none`

---

## Следующие задачи (если потребуется)

### 🔄 Потенциальные улучшения:

- [ ] Добавить анимацию появления параболических линий
- [ ] Настроить высоту изгиба в зависимости от высоты блоков
- [ ] Добавить интерактивность (hover эффекты на кружках)
- [ ] Оптимизировать перерасчет координат (debounce для resize)
